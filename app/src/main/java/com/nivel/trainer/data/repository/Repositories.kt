package com.nivel.trainer.data.repository

/*
 * Репозитории — единый вход для UI, склейка remote (NivelApi) + local (Room-кэш).
 * Появляются в B3 и далее. Пакет создан в scaffold-задаче B1 под целевую структуру.
 */
